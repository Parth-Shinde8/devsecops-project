# Use lightweight Node image
FROM node:18-alpine

# Create app directory
WORKDIR /app

# Copy package files first (for caching)
COPY package*.json ./

# Install dependencies
RUN npm install --only=production

# Copy remaining code
COPY . .

# Expose port (posts service runs on 4000)
EXPOSE 4002

# Start app
CMD ["npm", "start"]